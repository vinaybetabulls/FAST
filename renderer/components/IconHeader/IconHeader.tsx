import React from "react";

const IconHeader = () => {
  return (
    <div style={{ background: "white", color: "#000", padding: "0 8px" }}>
      <h1>IconHeader</h1>
    </div>
  );
};

export default IconHeader;
